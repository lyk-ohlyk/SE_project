<%= form_for(current_user.relatecourses.find_by(lesson_id: @course.id),
             remote: true,
             html: { method: :delete }) do |f| %>
    <%= f.submit 'Remove', class: "btn btn-large" %>
<% end %>